# Runtime mode
ENV=production
NODE_ENV=production
PORTFOLIO_ENV=production

# Primary public domain
DOMAIN=jargonless.ai

# LAN test ports (leave as-is; Cloudflare Tunnel does not require them, but they help for local checks)
HOST_HTTP_PORT=8080
HOST_API_PORT=8000

# Compose project name
COMPOSE_PROJECT_NAME=portfolio